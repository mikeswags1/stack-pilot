# StackPilot — Agent Collaboration Log

This file is shared between **Claude**, **Codex**, **Cursor**, and **Antigravity**.
Read it before starting any work. Update it when you finish.

## Agent Workflow

1. Read `COLLAB.md` before starting work.
2. Keep edits scoped and avoid overwriting another agent's active files.
3. Update `COLLAB.md` after finishing important changes, deploys, or blockers.

---

## 🔴 Currently In Progress
_Clear this section when done._

| Agent | File(s) | What | Started |
|-------|---------|------|---------|
| Mike | Launch readiness / Mike's account | **Do not treat StackPilot as live-ready until the product sourcing/listing flow is perfectly smooth on Mike's own account.** Admin and Product Listing must stay consistent: if Admin says a niche is truly Done, Product Listing must present 30 list-ready products for that niche. Mike confirmed `Coins & Currency`, `Fitness Equipment`, and `Clothing & Accessories` currently show 30 as expected; continue validating more niches and fixing any mismatch before launch. | 2026-05-23 |
| Mike + Claude | Vercel env vars | **Enabling RapidAPI Real-Time Amazon Data (Pro $25/mo)** as primary Amazon PDP enrichment source. Adding `RAPIDAPI_KEY` + `ENABLE_RAPIDAPI_FALLBACK=1`. Code path already exists in `lib/amazon-product.ts::fetchProductDetailsFromApi`. Once live, will trigger `/api/admin/enrich-pool` in a loop to enrich the ~5,748 unenriched products currently in the active pool. | 2026-05-22 |
| Mike | Amazon Associates enrollment | **Long-term goal: replace RapidAPI with free Amazon PA-API 5.0.** Mike enrolling in Amazon Associates program in parallel. Requires 3 qualifying affiliate sales within 180 days to gain PA-API access (2-6 week realistic wait). Once approved, switch primary enrichment tier to PA-API and either cancel or downgrade RapidAPI. PA-API integration code NOT YET written — todo for whichever agent picks this up when PA-API access is granted. | 2026-05-22 |

---

## ✅ Recently Completed

| Date | Agent | What Was Done | Key Files |
|------|-------|---------------|-----------|
| 2026-05-23 | Codex | Added a preview-first "Clean Dead Listings" control in the Scripts tab. It fetches live eBay active-listing signals, finds listings that are 14+ days old with 0 sold, 0 watchers, and 2 or fewer views, shows a preview, then requires confirmation before ending up to 100 poor performers per run and marking only successfully-ended rows as ended locally. | `app/api/ebay/dead-listings/route.ts`, `app/dashboard/components/ScriptsTab.tsx`, `COLLAB.md` |
| 2026-05-23 | Codex | Tightened `/admin` niche readiness semantics so "Done" now means a niche has 60+ backend publish-ready products, giving Product Listing enough buffer to reliably present 30 after its final preflight filters. Added a Product Listing expectation column (`30 shown`, `Usually 30, no buffer`, or max count) so Admin is the source of truth for whether a niche is truly safe to rely on. | `app/admin/page.tsx`, `COLLAB.md` |
| 2026-05-23 | Codex | Reverted Product Listing niche picker back to the simple grouped UI and added the missing source niches to that list (cable/desk/garage/refrigerator/closet/hobby variants). Kept readiness/status reporting out of Product Listing per Mike's direction. Admin now lists all niche readiness rows instead of only the top 18 and labels them Admin-only as Done (45+ publish-ready), Thin (30-44), or Repairing (<30), with summary counts at the top of the publishing funnel. | `app/dashboard/components/ProductListingTab.tsx`, `app/admin/page.tsx`, `COLLAB.md` |
| 2026-05-22 | Codex | Made `/admin` product sourcing numbers more explicit. Added a dedicated publish-readiness funnel to admin stats so "source pool" no longer gets confused with "ready to publish": active source rows → pass price/risk → enriched with cached 2+ images → publish-ready after excluding already-listed ASINs. `/admin` now shows a headline "Publish-Ready Now" card, a detailed funnel with blocked/needs-enrichment/unavailable/already-listed counts, and per-niche publish-ready vs needs-enrichment counts. Verified with TypeScript and ESLint. | `app/api/admin/stats/route.ts`, `app/admin/page.tsx`, `COLLAB.md` |
| 2026-05-22 | Codex | Closed the remaining one-image listing escape hatch after RapidAPI enrichment was re-enabled. Product Finder/dashboard/source-engine already required enriched products with 2+ images, but `/api/ebay/list-product` still had `MIN_LISTING_IMAGES = 1`, so stale tabs/direct publish/Auto Bulk could publish a one-image listing if enrichment missed. Raised final publish gate to 2 images, updated the admin enrich endpoint comments, fixed the list-ready audit script to count only cached 2+ image products, and removed a stale lint directive. Production audit with the stricter gate shows 1,487 list-ready products across top 30 niches; every audited niche has at least 30 ready products. | `app/api/ebay/list-product/route.ts`, `app/api/admin/enrich-pool/route.ts`, `scripts/audit-listable-after.mjs`, `app/api/scripts/product-finder/route.ts`, `COLLAB.md` |
| 2026-05-22 | Claude | **REVERTED dashboard image gate to ≥2** per user directive. Earlier in same session lowered `isPublishReadyProduct` image gate from 2 → 1 to unblock "list ready (5)" count; user reminded me single-image listings are explicitly forbidden (see flag line 153 — "Bulk listing preflight: skips <2 images. Do NOT remove."). Image gate reverted. Real fix for low list-ready counts is enrichment throughput, NOT lowering the bar. `MIN_STOCK_PROFIT 9→4` and `MIN_STOCK_ROI 32→25` were ALSO changed and KEPT — those align the dashboard filter with the source engine's floor (commit 3ecf793) and don't compromise quality (engine still enforces profit ≥3). | `app/api/scripts/product-finder/route.ts`, `COLLAB.md` |
| 2026-05-22 | Claude | Added `POST /api/admin/enrich-pool` admin endpoint calling `warmAmazonProductCache(limit)` (max 200 per invocation). Mass-enriches `amazon_product_cache` for the 5,748 pool rows that currently only have `image_url` (search-result image, 1 image). Initial test run: 80 attempted → 13 warmed, 67 failed (~16% success rate due to Amazon bot detection). Use `node scripts/enrich-batch.mjs [limit]` to run repeatedly. Improving enrichment success rate is now the right path to raise list-ready counts (since the ≥2 image gate stays). | `app/api/admin/enrich-pool/route.ts`, `scripts/enrich-batch.mjs`, `COLLAB.md` |
| 2026-05-22 | Claude | Added `POST /api/admin/backfill-scores` admin endpoint calling new `backfillProductScores(options)` in `product-source-engine.ts`. Targets active pool rows with NULL master_score or score below MIN_MASTER_SCORE (38). One-shot ran on prod: scanned 2,778 unscored rows, updated all 2,778 (0 deactivated, 0 skipped, 10.8s). Tier-B (65-79) products grew from 1,457 → 2,475. Tier-A (≥80) is still 0 — current scoring weights cap at ~78; recalibration optional. | `lib/product-source-engine.ts`, `app/api/admin/backfill-scores/route.ts`, `scripts/check-score-distribution.mjs`, `COLLAB.md` |
| 2026-05-22 | Claude | Updated default Anthropic model from `claude-3-5-haiku-latest` (returned 404 in production — alias retired) to `claude-haiku-4-5` in both `lib/source-agent.ts` and `lib/digest-agent.ts`. Production should still set `ANTHROPIC_MODEL` env var explicitly to a current dated model id from console.anthropic.com (Models tab) for stability — the code default is only a fallback. | `lib/source-agent.ts`, `lib/digest-agent.ts`, `COLLAB.md` |
| 2026-05-22 | Claude | Expanded `TRENDING_NEW_NICHE_QUERIES` with 90+ new Amazon search queries for supply-starved niches: Travel Accessories (+25), Backyard & Patio (+24), Beach & Sunny Day (+22), Golf Accessories (+22). All queries focused on margin-friendly characteristics: lightweight, sub-$60 retail, practical/useful, non-branded (avoid VeRO). Mixed seasonal + year-round so niches don't dry up post-summer. | `lib/source-niches.ts`, `COLLAB.md` |
| 2026-05-22 | Claude | Aligned dashboard niche filter `loadProductSourceProducts` with `pool-status` thresholds. Was applying stricter (profit≥7, roi≥30) than the diagnostic endpoint (profit≥3, roi≥25), causing 5 seasonal niches (Pool, Beach, Golf, Travel, Backyard) to show fewer than 30 products on the dashboard. Lowered to profit≥4, roi≥25 across all 4 query branches (niche-specific and continuous, with and without excludeAsins). Pool Products jumped 28 → 80+ visible; the remaining few sub-30 niches are genuinely supply-starved (Source Agent territory). | `lib/product-source-engine.ts`, `scripts/audit-niche-counts.mjs`, `COLLAB.md` |
| 2026-05-22 | Claude | Added eBay category lookup cache (`lib/ebay-category-cache.ts` + new `ebay_category_cache` table keyed by ASIN, 14-day TTL). Each listing previously fired 4 parallel eBay category API calls (getCategoryByAsin, getCategoryByComparableListings, getTaxonomyCategoryIds, getSuggestedCategoryIds). At 100+ listings/day that's 400+ category calls contributing to "quota exceeded" failures. Cache populated only after eBay accepts the listing (post-INSERT into listed_asins), so cached entries are always verified-good. On cache hit, all 4 API calls are skipped — the cached categoryId and leaf-suggested fallback IDs feed straight into the existing retry chain, which self-heals if eBay later invalidates the cached category. Expected ~75% reduction in eBay category API calls. | `lib/ebay-category-cache.ts`, `app/api/ebay/list-product/route.ts`, `COLLAB.md` |
| 2026-05-22 | Claude | Fixed two failure modes responsible for ~70% of recent auto-listing failures: (1) `EBAY_LISTING_FAILED` Picture URL too long — added total-length cap (3700 chars) accounting for `&` → `&amp;` XML escape inflation; existing per-URL 500-char cap kept. (2) `ASIN_MISMATCH` / `PRODUCT_UNAVAILABLE` — queue entries scored against stale source data fail when ASINs drift on Amazon. Added 24h TTL on `auto_listing_queue` entries via new `expireStaleQueueEntries(userId)` and a sweep call in `acquireNextDueJob`. Adds `scripts/audit-stackpilot.mjs` for live DB diagnostic queries. | `app/api/ebay/list-product/route.ts`, `lib/auto-listing/queue.ts`, `scripts/audit-stackpilot.mjs`, `COLLAB.md` |
| 2026-05-20 | Claude | Fixed product pool: 3 root causes. (1) `rowToProduct` set `available = undefined` when no `amazon_product_cache` row existed — `isPublishReadyProduct` requires `available === true`, so ALL pool products without a cache entry showed `publishReadyCount = 0`, disabling "List All". Fix: `available: row.cached_available ?? true` (assume available when unverified). (2) All 4 SQL branches in `loadProductSourceProducts` had `AND last_seen_at > NOW() - INTERVAL '21 days'` — valid products older than 21 days were silently excluded, shrinking sparse niches to near-zero. Removed the staleness filter entirely (products deactivated by cron when truly unavailable). (3) Added `NOT EXISTS (SELECT 1 FROM listed_asins la WHERE UPPER(la.asin) = UPPER(psi.asin) AND la.ended_at IS NULL)` to all 4 SQL branches — previously already-listed ASINs leaked through if they were not in the current session's excludeAsins. Added `scripts/diagnose-pool.mjs`, `scripts/verify-fix.mjs`, and `/api/debug/pool-status` endpoint for future diagnostics. TypeScript: 0 errors. | `lib/product-source-engine.ts`, `app/api/debug/pool-status/route.ts`, `scripts/diagnose-pool.mjs`, `scripts/verify-fix.mjs`, `COLLAB.md` |
| 2026-05-20 | Claude | Fixed niche batching and continuous listing system — 4 root causes addressed: (1) `loadProductSourceProducts` now accepts an `excludeAsins` parameter and filters them in the SQL query using `UPPER(psi.asin) <> ALL(...)`, fetching extra rows to compensate; (2) product-finder route now passes `excludeAsins` into `loadProductSourceProducts` so genuinely fresh products are returned at the DB level, not just client-side filtered; (3) `mergeRefilledProducts` now self-heals when incoming products are all duplicates — uses incoming as fresh replacement instead of returning a stuck/empty pool; (4) continuous auto-reseed now passes the currently-queued ASINs as excludes instead of blindly re-fetching. Added logging at all critical junctions (fetchFinderProducts, mergeRefilledProducts, refillNiche/Continuous, sourceEngine load/result, continuousAutoReseed). | `lib/product-source-engine.ts`, `app/api/scripts/product-finder/route.ts`, `app/dashboard/page.tsx`, `app/dashboard/api.ts`, `COLLAB.md` |
| 2026-05-20 | Claude | Fixed stale/rejected ASINs re-entering source pool: `upsertProductSourceItems` ON CONFLICT set `active = TRUE` unconditionally, resurrecting ASIN_MISMATCH/unavailable rejects on every 30-min pool rebuild. Fix 1: ON CONFLICT now preserves `active = FALSE` when `source_quality = 'reject'`. Fix 2: `rebuildProductSourceFromCache` now loads all rejected/inactive ASINs first and skips them before upsert. | `lib/product-source-engine.ts`, `COLLAB.md` |
| 2026-05-20 | Claude | Fixed bulk listing (0 listed · 30 failed): `checkAmazonLiveAvailability` scraped Amazon live for every product in a 30-item batch, triggering rate-limiting that returned the bot-detection page for every call → `CHECK_FAILED` on all 30. Fix: trusted mode (List All / Auto Bulk) now skips the live scrape and uses already-validated cache/pool data as the availability result. Non-trusted single-listing still gets the live check (one call, safe). | `app/api/ebay/list-product/route.ts`, `COLLAB.md` |
| 2026-05-20 | Claude | Fixed build error: `digest-agent.ts` returned `ok: true` which duplicated the field that `apiOk()` already adds, causing TS2783. Removed `ok: true` from both return paths. Zero TypeScript errors confirmed. | `lib/digest-agent.ts`, `COLLAB.md` |
| 2026-05-20 | Claude | Improved product sourcing pipeline with three additive scoring signals: (1) BSR extraction — parses Best Seller Rank from Amazon product pages (`scrapeAmazonProduct`) and stores in `amazon_product_cache.best_seller_rank`; baked into `scoreProduct()` as ×1.25/×1.15/×1.07/×0.91 multiplier; (2) eBay competition check — new `lib/ebay-competition.ts` queries eBay Finding API for active fixed-price listing count per product title, stores in `product_source_items.ebay_competitor_count`, penalizes saturated items (×0.60 for >150 competitors, ×1.05 for <10); runs 20 products per self-healing cycle; (3) Listing outcome feedback — `applyListingOutcomeFeedback()` reads `auto_listing_queue` 60-day history and writes `listing_outcome_score` [0.60–1.25] per ASIN; successful sellers get boosted, repeated failures get penalized. All three multipliers applied in both TypeScript `scoreProduct()` and SQL `applySourceIntelligenceScores`. | `lib/amazon-scrape.ts`, `lib/amazon-product.ts`, `lib/product-source-engine.ts`, `lib/source-intelligence.ts`, `lib/ebay-competition.ts`, `COLLAB.md` |
| 2026-05-20 | Claude | Added three 24/7 backend AI agents: (1) Reprice Agent (`/api/cron/reprice-agent`, hourly at :45) — joins `listed_asins` with `amazon_product_cache`, recalculates optimal eBay price when Amazon cost drifts >$0.50 or >3%, calls eBay `ReviseFixedPriceItem` API, logs to `reprice_agent_log`; (2) Fulfillment Agent (`/api/cron/fulfillment-agent`, every 20 min) — polls eBay pending orders for all users, looks up ASIN from `listed_asins` by `legacyItemId`, pre-stages `fulfillment_jobs` so browser extension requires no manual "Start Fulfillment" click per order, tracks in `fulfillment_agent_tracker`; (3) Digest Agent (`/api/cron/digest-agent`, daily 06:00 UTC) — aggregates system metrics, generates AI summary via Anthropic/OpenRouter, stores in `performance_digests` with once-per-day guard. All three support `?dryRun=1` and `CRON_SECRET` auth. | `lib/reprice-agent.ts`, `lib/fulfillment-agent.ts`, `lib/digest-agent.ts`, `app/api/cron/reprice-agent/route.ts`, `app/api/cron/fulfillment-agent/route.ts`, `app/api/cron/digest-agent/route.ts`, `vercel.json`, `COLLAB.md` |
| 2026-05-20 | Codex | Added a sourcing-only AI Source Agent for 24/7 backend product intelligence. The new cron can use Claude via `ANTHROPIC_API_KEY` or OpenRouter fallback to create safe new source niches, choose weak niches for repair, clean bad source rows, and trigger source refreshes without any ability to publish eBay listings. Admin stats now expose the agent provider and recent runs. | `lib/source-agent.ts`, `app/api/cron/source-agent/route.ts`, `app/api/admin/stats/route.ts`, `app/admin/page.tsx`, `vercel.json`, `.env.example`, `COLLAB.md` |
| 2026-05-20 | Codex | Fixed the Product Finder live-verification build error and adjusted dashboard rotation so Product Listing shows ready products first but still fills the visible grid back toward 30 with sourced candidates instead of collapsing to only the ready subset. Deployed production commit `3147cb5` and ran a Garden & Tools stock repair, which refreshed 3,571 source products and deactivated 1,713 unavailable source rows. | `app/api/scripts/product-finder/route.ts`, `app/dashboard/page.tsx`, `COLLAB.md` |
| 2026-05-20 | Codex | Tightened Product Finder readiness so products are only treated as bulk-listable after live Amazon buyability verification, added a live-fill pass that scrapes/validates more niche candidates when fewer than 30 ready products are available, and made dashboard bulk preflight skip unverified products instead of letting publish fail them. | `app/api/scripts/product-finder/route.ts`, `app/dashboard/utils.ts`, `COLLAB.md` |
| 2026-05-20 | Codex | Fixed Product Listing appearing empty for weak niches after the publish-ready filter: dashboard now falls back to showing sourced candidates when none are fully publish-ready yet, List All only attempts preflight-ready products, and Product Finder schedules a stockWeak repair for niches with too few ready products. | `app/dashboard/page.tsx`, `app/api/scripts/product-finder/route.ts`, `COLLAB.md` |
| 2026-05-20 | Codex | Set new eBay listings to ship from King City, California (`93930`) directly in the publish XML so production env defaults cannot fall back to the old New Jersey location. | `app/api/ebay/list-product/route.ts`, `COLLAB.md` |
| 2026-05-20 | Codex | Tightened Product Listing queue quality after live List All still skipped one-image products: source-engine rows now hydrate cached Amazon images/content before ranking, Product Finder actively enriches top sparse candidates before returning them and rejects unavailable/mismapped ASINs, and the dashboard rotation/List All view now exposes only products that pass bulk preflight. | `lib/product-source-engine.ts`, `app/api/scripts/product-finder/route.ts`, `app/dashboard/page.tsx`, `COLLAB.md` |
| 2026-05-20 | Codex | Improved Product Finder ranking so the backend prioritizes publish-ready products first: `source_quality='ready'`, 2+ real images, richer cached content, low risk, minimum profit, and minimum ROI now score above sparse/stale candidates. Finder now drops cache-confirmed ASIN title mismatches before they reach the grid, and publish marks mismatch/unavailable ASINs as rejected in the source pool so they do not keep coming back. | `app/api/scripts/product-finder/route.ts`, `app/api/ebay/list-product/route.ts`, `COLLAB.md` |
| 2026-05-20 | Codex | Hardened the Product Listing publish backend so `/api/ebay/list-product` now blocks an ASIN if it is already active anywhere on StackPilot, not only on the current user's account. Product Finder already filtered cross-user active ASINs; this makes publish the final safety gate for stale tabs, direct API calls, and background listing paths. | `app/api/ebay/list-product/route.ts`, `COLLAB.md` |
| 2026-05-15 | Codex | Added automatic niche cache stocking for `/admin` Needs Stock rows: source intelligence now prioritizes low/stale niche caches, `/api/cron/refresh-products` supports a lightweight `stockWeak=1` repair mode that merges fresh Amazon finds with existing niche cache instead of replacing good inventory with tiny scrape results, Vercel Pro now runs the stocker hourly, admin autopilot uses the fast stocker for weak niches, and `/admin` shows Stock Weak Niches / Stock now controls plus clearer automation copy. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `lib/source-intelligence.ts`, `app/api/cron/refresh-products/route.ts`, `app/api/admin/stats/route.ts`, `app/api/admin/refresh-pool/route.ts`, `app/admin/page.tsx`, `vercel.json`, `COLLAB.md` |
| 2026-05-15 | Codex | Clarified `/admin` source-engine metrics so Product Pool and daily sourcing no longer look like duplicate 15k counts. `New products today` now uses actual `product_source_items.first_seen_at` rows from today, `Ready/listable pool` names the current vetted inventory, `Cleaned today` names removed/problem source rows, and recent source jobs now show pool impact instead of misleading “found” copy. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `lib/source-intelligence.ts`, `app/admin/page.tsx`, `COLLAB.md` |
| 2026-05-15 | Codex | Added source-maintenance cleanup for unavailable local-only listing rows: if a `listed_asins` row has no eBay listing id and Amazon cache confirms unavailable, cron now marks it ended locally so Orders/Financials/Admin do not treat dead imported rows as active listings. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `app/api/cron/refresh-products/route.ts`, `COLLAB.md` |
| 2026-05-15 | Codex | Added eBay active-listing reconciliation to the 30-minute `sourceOnly` source-maintenance cron. Pre-test DB sweep found source pool/continuous cache healthy but existing active listings with Amazon-cache unavailable flags; source maintenance now syncs eBay active IDs before availability cleanup so stale local active listings get marked ended more consistently. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `app/api/cron/refresh-products/route.ts`, `COLLAB.md` |
| 2026-05-15 | Codex | Fixed the production source-maintenance cron Neon parameter error found during post-deploy verification: stale-product price refresh now uses a typed interval expression, and remaining untyped array `ANY(...)` queries in cron, Product Finder, and fulfillment state lookup now cast arrays explicitly. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `lib/product-source-engine.ts`, `app/api/cron/refresh-products/route.ts`, `app/api/scripts/product-finder/route.ts`, `lib/fulfillment.ts`, `COLLAB.md` |
| 2026-05-15 | Codex | Added an Admin Automation Command Center so launch operations clearly shows Pro cron cadence, Auto Bulk users/queue/due jobs/listed-24h, active Amazon availability audit backlog, and recent automation timestamps. Settings Auto Bulk copy now tells users the 15-minute cadence and what Pause/Emergency Stop do. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `app/api/admin/stats/route.ts`, `app/admin/page.tsx`, `app/dashboard/components/SettingsTab.tsx`, `app/globals.css`, `COLLAB.md` |
| 2026-05-15 | Codex | Reconfigured background automation for Vercel Pro: product maintenance now runs every 30 minutes, deep catalog crawling every 6 hours, and Auto Bulk Listing ticks every 15 minutes. Auto Bulk cron is no longer hard-coded to `userId=4`; without a `userId` it rotates across enabled users with active eBay credentials, while preserving per-user locks, cooldowns, max/hour caps, and emergency stops. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `vercel.json`, `app/api/cron/auto-listing-tick/route.ts`, `COLLAB.md` |
| 2026-05-15 | Codex | Ran launch-readiness audit for product readiness: lint/typecheck/build/live smoke passed, production deploy was Ready on commit `2665a2b`, source pool showed 15k+ listable products, and publish flow was confirmed to live-check Amazon buyability before eBay. Hardened active-listing safety by raising the default Amazon listing audit sweep from 16 to 60 and making production cron pass `auditLimit=60`, so stale/unavailable active listings are checked and ended faster. | `app/api/cron/refresh-products/route.ts`, `vercel.json`, `COLLAB.md` |
| 2026-05-15 | Codex | Added source autopilot so admins do not have to babysit Deep Catalog Crawl: admin stats now detects weak/stale/low-stock source health, reserves a throttled repair slot, triggers the existing refresh-products route in the background with `autopilot=1`, records autopilot runs separately in source history, and shows Autopilot status/next allowed repair/target niches in `/admin`. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `lib/source-intelligence.ts`, `app/api/admin/stats/route.ts`, `app/api/cron/refresh-products/route.ts`, `app/admin/page.tsx`, `app/globals.css` |
| 2026-05-13 | Codex | Built the source engine intelligence layer: product-source rows now get intelligence scores/source-quality labels, weak niches are prioritized during catalog refreshes, stale/bad/unavailable products are self-healed, Product Finder/Auto Bulk candidates filter rejected and cache-confirmed unavailable ASINs, Continuous cache skips unavailable/already-live ASINs, source refresh runs are logged, and `/admin` now shows source intelligence status, recommendations, and recent job history. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `lib/source-intelligence.ts`, `lib/product-source-engine.ts`, `lib/auto-listing/scoring.ts`, `app/api/cron/refresh-products/route.ts`, `app/api/admin/stats/route.ts`, `app/api/product-source/health/route.ts`, `app/api/setup-db/route.ts`, `app/admin/page.tsx`, `app/globals.css` |
| 2026-05-12 | Codex | Expanded StackPilot's source engine for production launch: added summer/trending niches, trend-aware scoring boosts/penalties, admin-managed custom source niches, per-niche source refresh, a clearer `/admin` source-flow audit view, and Trending Niches health with product counts, profit/ROI averages, cache freshness, risk/image issues, and status labels. Product Finder and the catalog cron now merge built-in trending niches plus admin-added niches. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, `git diff --check`, production deploy `dpl_GZ2yrnqvizMrW8XRUYUTzcWbStXv`, alias `stackpilot-app.vercel.app`, and live smoke passing on commit `527cd37`. | `lib/source-niches.ts`, `lib/product-source-engine.ts`, `app/api/scripts/product-finder/route.ts`, `app/api/cron/refresh-products/route.ts`, `app/api/admin/source-niches/route.ts`, `app/api/admin/stats/route.ts`, `app/api/admin/refresh-pool/route.ts`, `app/admin/page.tsx`, `app/globals.css`, `app/dashboard/components/ProductListingTab.tsx`, `app/dashboard/components/SettingsTab.tsx`, `app/api/ebay/list-product/route.ts` |
| 2026-05-12 | Codex | Added a hard live Amazon buyability gate before eBay publish so trusted/List All cannot use stale cached availability, strengthened Amazon scrape detection for no-offer/unavailable pages, and added a cron active-listing audit that marks/end listings when Amazon later becomes unavailable. Audit now prioritizes newest unchecked listings, supports `auditLimit` for manual sweeps, and source rebuilds deactivate any ASINs cached as unavailable. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `lib/amazon-availability.ts`, `lib/amazon-scrape.ts`, `lib/amazon-product.ts`, `lib/product-source-engine.ts`, `lib/listed-asins.ts`, `app/api/ebay/list-product/route.ts`, `app/api/cron/refresh-products/route.ts`, `COLLAB.md` |
| 2026-05-12 | Codex | Fixed Product Listing showing only a few products while source health showed hundreds: Amazon search scraping now extracts real `h2` listing titles instead of brand-only card text, weak titles are rejected before/while loading source items, Product Finder pulls deeper from the niche source pool, and Settings top niches now show ready/listable counts instead of raw source rows. Verified Jewelry & Watches had 593 raw rows but 86 usable title rows, enough for a 30-item queue. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `lib/amazon-scrape.ts`, `lib/product-source-engine.ts`, `app/api/scripts/product-finder/route.ts`, `app/api/product-source/health/route.ts`, `app/dashboard/components/SettingsTab.tsx`, `COLLAB.md` |
| 2026-05-12 | Codex | Removed RapidAPI as a hard dependency for product sourcing. Product Finder now continues live refill instead of returning tiny fresh caches, targeted low-stock niche repair calls the cron for the exact niche, direct Amazon scrape runs in parallel batches, the catalog cron can refresh a requested niche, and RapidAPI-backed calls are gated behind `ENABLE_RAPIDAPI_FALLBACK=1` as optional fallback only. Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. | `app/api/scripts/product-finder/route.ts`, `app/api/cron/refresh-products/route.ts`, `app/dashboard/page.tsx`, `lib/rapidapi.ts`, `lib/amazon-product.ts`, `lib/amazon-mapping.ts`, `lib/product-source-engine.ts`, `app/api/financials/route.ts`, `app/api/ebay/list-product/route.ts`, `app/api/fulfillment/lookup/route.ts`, `app/api/product-source/health/route.ts`, `.env.example`, `COLLAB.md` |
| 2026-05-12 | Codex | Synced free-trial display wording across dashboard meters and Settings billing: all trial UI now renders the same `used / limit used · remaining left` count, passes server `trialRemaining` into every `TrialMeter`, and refreshes subscription status when Settings opens. Verified with `npm run lint`, `npm run typecheck`, and `npm run build`. | `app/dashboard/components/TrialMeter.tsx`, `app/dashboard/components/SettingsTab.tsx`, `app/dashboard/components/DashboardTopbar.tsx`, `app/dashboard/components/OverviewTab.tsx`, `app/dashboard/components/CompactHomeTab.tsx`, `app/dashboard/components/ProductListingTab.tsx`, `app/dashboard/components/ContinuousListingTab.tsx`, `app/dashboard/page.tsx`, `COLLAB.md` |
| 2026-05-12 | Codex | Fixed Settings Auto Bulk product-pool readability by replacing the single native niche dropdown with a readable in-app multi-select checklist. Users can now choose `All Niches`, one niche, or multiple niches. Also changed the background catalog cron to daily because Vercel Hobby blocks hourly cron deployments. Verified with `npm run lint`, `npm run typecheck`, and `npm run build`. | `app/dashboard/components/SettingsTab.tsx`, `app/globals.css`, `vercel.json`, `COLLAB.md` |
| 2026-05-11 | Codex | Cleaned Settings further after dropdown readability feedback: moved Auto Bulk above the technical source-health panel, made its visible controls product pool/mode/account only, tucked numeric limits into a collapsible section, and added dedicated dark readable select/option styling. Verified with `npm run lint`, `npm run typecheck`, and `npm run build`. | `app/dashboard/components/SettingsTab.tsx`, `app/globals.css`, `COLLAB.md` |
| 2026-05-11 | Codex | Cleaned up Settings UI and Auto Bulk Listing controls: slimmer eBay/billing/source-health cards, clearer Auto Bulk status metrics, and replaced typed desired niches with a dropdown whose first option is `All Niches` (saved as unrestricted). Verified with `npm run lint`, `npm run typecheck`, and `npm run build`. | `app/dashboard/components/SettingsTab.tsx`, `COLLAB.md` |
| 2026-05-11 | Codex | Fixed Stripe Checkout "Not a valid URL" from Settings billing by normalizing the app origin before building Stripe success/cancel URLs, including missing-protocol env values and a production fallback. Verified with `npm run lint`, `npm run typecheck`, and `npm run build`. | `lib/app-url.ts`, `COLLAB.md` |
| 2026-05-11 | Codex | Verified the restored Stripe production env names in Vercel, redeployed production so billing keys are loaded, re-pointed `stackpilot-app.vercel.app` at the fresh deployment, and confirmed live smoke plus `/api/stripe/checkout` now reaches the auth gate instead of a billing setup error. | Vercel env/deploy, `COLLAB.md` |
| 2026-05-11 | Codex | Fixed billing/trial sync: Settings always shows the Stripe upgrade action instead of hiding it behind env-state, checkout accepts common Pro price env aliases, trial usage now counts only StackPilot trial publishes (not imported old eBay listings), and successful publish responses immediately update the free-trial counter in the dashboard. Verified with `npm run lint`, `npm run typecheck`, and `npm run build`. | `app/dashboard/components/SettingsTab.tsx`, `app/dashboard/page.tsx`, `app/dashboard/types.ts`, `app/api/ebay/list-product/route.ts`, `app/api/stripe/checkout/route.ts`, `lib/subscription.ts`, `lib/stripe.ts`, `.env.example` |
| 2026-05-11 | Codex | Fixed unavailable Amazon products leaking into listing flow: finder now filters cache-confirmed unavailable ASINs, bulk preflight skips unavailable queue items, single-list validation removes/refills unavailable products before publish, and eBay publish deactivates unavailable sources for future queues. Verified with `npm run lint`, `npm run typecheck`, and `npm run build`. | `app/dashboard/page.tsx`, `app/dashboard/components/SellOnEbayModal.tsx`, `app/dashboard/types.ts`, `app/dashboard/utils.ts`, `app/api/scripts/product-finder/route.ts`, `app/api/ebay/list-product/route.ts` |
| 2026-05-10 | Cursor | Merged **`origin/master`** into banner work and **deduped** install UI: removed Codex **`GetAppBanner.tsx`** in favor of **`GetTheAppBanner` / `Inner`** only — desktop card stays in **sidebar**; compact shell unchanged; homepage **`#get-app`** wraps hero card (nav link). | `GetTheAppBanner*.tsx`, `DashboardSidebar.tsx`, `app/page.tsx`, `app/dashboard/page.tsx`, `globals.css`, `COLLAB.md` |
| 2026-05-10 | Codex | Enforced the free-trial listing cap as a lifetime per-account counter: added `trial_listings_used`, made eBay publish reserve trial slots atomically before AddFixedPriceItem, release slots on failed publish attempts, block stale/direct API listing after the free trial is used, refresh trial usage after publishing, and updated dashboard/guide/billing copy so trial users cannot list after the cap. Verified with `npm run lint`, `npm run typecheck`, and `npm run build`. | `lib/subscription.ts`, `app/api/ebay/list-product/route.ts`, `app/api/subscription/status/route.ts`, `app/api/setup-db/route.ts`, `app/dashboard/page.tsx`, `app/dashboard/components/ProductListingTab.tsx`, `app/dashboard/components/ContinuousListingTab.tsx`, `app/dashboard/components/SellOnEbayModal.tsx`, `app/dashboard/components/TrialMeter.tsx`, `app/dashboard/components/SettingsTab.tsx`, `app/guide/page.tsx`, `docs/STRIPE_SETUP.md` |
| 2026-05-10 | Codex | Tightened account-scoped billing/trial sync: subscription status now normalizes inactive Pro rows back to trial, `/api/subscription/status` returns `isPro` and `trialRemaining`, free-trial meters only show for trial accounts and can show 0 left, Settings shows exact account plan/usage, and desktop/mobile menus show Trial/Pro badges next to the logged-in account. | `lib/subscription.ts`, `app/api/subscription/status/route.ts`, `app/dashboard/page.tsx`, `app/dashboard/components/DashboardSidebar.tsx`, `app/dashboard/components/CompactPwaMenu.tsx`, `app/dashboard/components/SettingsTab.tsx`, `app/dashboard/components/TrialMeter.tsx`, `app/dashboard/api.ts`, `app/globals.css` |
| 2026-05-10 | Codex | Synced Cursor/GPT's latest `master` work locally and verified the current live production commit `0c32418`: `npm run lint`, `npm run typecheck`, `npm run build`, and live smoke check against `https://stackpilot-app.vercel.app` all passed. No code changes were needed. | `COLLAB.md` |
| 2026-05-08 | Cursor | **“Get our app”** install hint (Add to Home Screen / PWA): `/`, `/login`, `/signup`; **desktop `/dashboard`** places it in the **left sidebar** (under Operations Dashboard, above eBay status). **Compact PWA**: under top bar in main. Dismiss `stackpilot:get_the_app:dismissed:v2`; test with `?installHint=1`. Hidden in standalone PWA. | `GetTheAppBanner*.tsx`, `app/dashboard/components/DashboardSidebar.tsx`, `app/dashboard/page.tsx`, `app/page.tsx`, `app/login/page.tsx`, `app/signup/page.tsx`, `globals.css`, `COLLAB.md` |
| 2026-05-08 | GPT-5.2 | **Release readiness tooling**: `/api/health` (DB ping + env flags, no secrets); `docs/RELEASE_CHECKLIST.md`; `npm run smoke` → `scripts/smoke-check.mjs`; CI runs **`npm run build`** with placeholder env for secrets-free GitHub Actions | `app/api/health/route.ts`, `docs/RELEASE_CHECKLIST.md`, `scripts/smoke-check.mjs`, `package.json`, `.github/workflows/ci.yml`, `COLLAB.md` |
| 2026-05-08 | GPT-5.2 | Fulfillment tab: in-tab Amazon extension setup card; fulfill URLs carry `stackpilotOrigin` for the extension API; broader extension host permissions | `app/dashboard/components/FulfillmentTab.tsx`, `app/api/fulfillment/start/route.ts`, `extension/manifest.json`, `extension/background.js`, `extension/INSTALL.md` |
| 2026-05-08 | GPT-5.2 | Host extension zip on StackPilot (`/stackpilot-fulfillment-extension.zip` via `prebuild`/`dev`); Fulfillment tab primary download button — no GitHub required | `scripts/zip-extension.mjs`, `package.json`, `app/dashboard/components/FulfillmentTab.tsx`, `.gitignore`, `extension/INSTALL.md` |
| 2026-05-08 | GPT-5.2 | Extension v0.2: persist ship-to in chrome.storage, try **Buy Now** on PDP, fill Amazon checkout widget IDs + retries; skip sign-in autofill | `extension/manifest.json`, `extension/background.js`, `extension/content.js`, `extension/INSTALL.md` |
| 2026-05-07 | Cursor/GPT | **Fulfillment (shipped):** Fulfillment **Queue** tab (awaiting shipment, ship-to copy, mapped ASIN); `lib/fulfillment` + `/api/fulfillment/{start,payload,status}`; Fulfill token URLs + optional-extension card + zip (`prebuild`/`dev`); **idempotent** payload (`getFulfillmentPayloadByToken`); fulfill URL **query + hash**; ship_to **normalize + flatten**; MV3 extension through **v0.3.2** — scaffold → `content-bootstrap.js` @ document_start, Buy Now + checkout + iframes, **Add new delivery address** priority, `clickAmazonControl`, service worker logging | `lib/fulfillment.ts`, `app/api/fulfillment/*`, `app/dashboard/types.ts`, `app/dashboard/constants.ts`, `app/dashboard/api.ts`, `app/dashboard/components/FulfillmentTab.tsx`, `app/dashboard/page.tsx`, `extension/*`, `scripts/zip-extension.mjs`, `package.json` |
| 2026-05-06 | Codex | Finished remaining launch polish queued in COLLAB (removed last generic `29223` publish fallback; added personalized first-name greeting on Overview) | `app/api/ebay/list-product/route.ts`, `app/dashboard/components/OverviewTab.tsx`, `app/dashboard/page.tsx` |
| 2026-05-06 | Codex | Added StackPilot PWA support — web manifest, standalone metadata, icons, service worker shell cache | `app/layout.tsx`, `app/manifest.ts`, `app/pwa-registration.tsx`, `app/globals.css`, `public/stackpilot-icon-192.png`, `public/stackpilot-icon-512.png`, `public/apple-touch-icon.png`, `public/sw.js` |
| 2026-05-06 | Codex | Added Problem Listings visibility to `/admin` with low-image/category warnings, issue tags, per-listing details | `app/admin/page.tsx`, `app/api/admin/stats/route.ts`, `app/globals.css` |
| 2026-05-06 | Codex | Added admin "Fix All" / repair-listings endpoint to backfill low-image records, recover missing categories | `app/admin/page.tsx`, `app/api/admin/repair-listings/route.ts`, `app/globals.css` |
| 2026-05-06 | Codex | Expanded `/admin` into full StackPilot control center (launch warnings, listing quality, pool/queue readiness, COLLAB viewer) | `app/admin/page.tsx`, `app/api/admin/stats/route.ts`, `app/globals.css` |
| 2026-05-06 | Codex | Polished private beta signup/request-access flow and premium public homepage copy/layout | `app/signup/page.tsx`, `app/login/page.tsx`, `app/page.tsx`, `app/layout.tsx`, `app/globals.css` |
| 2026-05-06 | Codex | Added live queue rotation for Product Listing / Continuous Listing: 60-product pool, show 30, reshuffle every second | `app/dashboard/page.tsx`, `app/dashboard/components/ProductListingTab.tsx` |
| 2026-05-06 | Codex | Refined StackPilot homepage (cleaner hero, removed distracting copy, airplane animation) | `app/page.tsx`, `app/globals.css`, `public/stackpilot-plane.svg` |
| 2026-05-06 | Codex | Added bulk listing preflight + replacement: skips weak titles/bad prices/low-profit/<2-image items before eBay publish | `app/dashboard/utils.ts`, `app/dashboard/page.tsx`, `app/dashboard/components/ProductListingTab.tsx`, `app/api/scripts/run/route.ts` |
| 2026-05-06 | Codex | Added CI checks (lint + typecheck), `.env.example`, normalized `/api/ai` with `OPENROUTER_*` env support | `.env.example`, `app/api/ai/route.ts`, `package.json`, `.eslintrc.json`, `.github/workflows/ci.yml` |
| 2026-05-06 | Antigravity | Fixed all NICHE_FALLBACK_LEAF_CATEGORY `29223` catch-alls — replaced with real eBay leaf category IDs (Clothing→11450, Jewelry→281, Coins→11116, Medical→51148, Car Parts→6030, Sporting→15273, Fishing→1492, Garden→2032, Safety→177742, Vintage→20081, Industrial→12576, Cleaning/Janitorial/Packaging→26677, Motorcycle→10063, Car Care→179716, Mental Wellness→26395, Fitness→15273) | `app/api/ebay/list-product/route.ts` |
| 2026-05-06 | Antigravity | Added personalized greeting to Overview tab — `"Good morning, Mike 👋"` via `userName` prop using `session.user.name` split on first space | `app/dashboard/components/OverviewTab.tsx`, `app/dashboard/page.tsx` |
| 2026-05-06 | Antigravity | Confirmed Performance tab already parallelized — `fetchOrders`, DB query, `fetchActiveListingMetrics` in `Promise.all`. No change needed. | `app/api/performance/route.ts` |
| 2026-05-06 | Antigravity | Updated COLLAB.md — added Cursor + Antigravity to agent roster, resolved merge conflict | `COLLAB.md` |
| 2026-05-06 | GPT-5.2 | Made `/admin` COLLAB viewer always live (no-store; force-dynamic) and confirmed `COLLAB.md` is the single canonical collaboration file | `app/api/admin/collab/route.ts`, `app/admin/page.tsx`, `COLLAB.md` |
| 2026-05-03 | Claude | Trusted mode title + price accuracy: after ASIN mismatch check, `validatedAmazon.title` replaced with `amazon_product_cache` title; price updated from cache if drift >10% | `app/api/ebay/list-product/route.ts` |
| 2026-05-03 | Claude | ASIN mismatch guard now universal — runs for ALL trusted listings regardless of image count | `app/api/ebay/list-product/route.ts` |
| 2026-05-03 | Claude | Campaigns Boost All fixed: correct eBay `bulk_create_ads_by_listing_id` body format | `app/api/ebay/campaigns/[campaignId]/listings/route.ts` |
| 2026-05-03 | Claude | Campaigns tab fixes: creation payload, empty/204 response, scope additions (`sell.marketing`), reconnect banner | `app/api/ebay/campaigns/route.ts`, `lib/ebay-auth.ts`, `app/dashboard/components/CampaignsTab.tsx` |
| 2026-05-03 | Claude | Loss-making listing block: catches both overpricing (>1.4× median) and underpricing (<0.62× median) | `app/api/ebay/list-product/route.ts` |
| 2026-05-02 | Claude | Image contamination fix + title punctuation + ASIN mismatch guard (non-trusted) | `lib/amazon-product.ts`, `app/api/ebay/list-product/route.ts` |
| 2026-05-02 | Claude | Title truncation strips trailing connectors/punctuation; trusted bulk >$40 runs live competitor check | `app/api/ebay/list-product/route.ts` |
| 2026-05-02 | Claude | Daily cron fixed: `vercel.json` changed to `?catalog=1&wait=1` — now runs full scraping (was no-op) | `vercel.json`, `app/api/cron/refresh-products/route.ts` |
| 2026-05-01 | Claude | 6-fix bulk listing pass: pricing, duplicate block, scraper fallback, warm cache, background enrichment, continuous concurrency | `app/api/ebay/list-product/route.ts`, `lib/product-source-engine.ts`, `lib/amazon-product.ts`, `app/api/cron/refresh-products/route.ts`, `app/api/scripts/product-finder/route.ts`, `app/dashboard/page.tsx` |
| 2026-05-01 | Claude | Amazon unavailability sync: block at listing + pool cleanup + auto-end listings daily | `app/api/ebay/list-product/route.ts`, `lib/product-source-engine.ts`, `app/api/cron/refresh-products/route.ts` |
| 2026-05-01 | Claude | Fix bulk listing: cache supplement in product-finder + list-product, block sparse fallback listings, concurrency 5→3 | `lib/amazon-product.ts`, `app/api/scripts/product-finder/route.ts`, `app/api/ebay/list-product/route.ts`, `app/dashboard/page.tsx` |
| 2026-05-01 | Claude | Fix wrong gallery images: removed `fetchedAmazon.images` from gallery entirely | `app/api/ebay/list-product/route.ts` |
| 2026-05-01 | Claude | Re-validate at publish for sparse images (<2): skip trusted mode, do full Amazon re-validation | `app/dashboard/page.tsx` |
| 2026-05-01 | Claude | Fix niche cursor: `Number('')` was 0; now uses `hasExplicitStart` flag | `app/api/cron/refresh-products/route.ts` |
| 2026-05-01 | Claude | Unblock supplements: only controlled substances remain blocked | `lib/listing-policy.ts` |
| 2026-05-01 | Claude | Cross-user ASIN dedup: product finder loads ALL users' active ASINs (limit 2,000) | `app/api/scripts/product-finder/route.ts` |
| 2026-05-01 | Claude | Niche rotation fix: catalog crawl picks 3 stalest niches by oldest `cached_at` | `app/api/cron/refresh-products/route.ts` |
| 2026-04-29 | Claude | BIG: own scraper replaces RapidAPI — `scrapeAmazonSearch`, 20 queries × 5 pages × 20 products = 2,500/niche | `app/api/cron/refresh-products/route.ts`, `app/api/admin/refresh-pool/route.ts`, `app/admin/page.tsx` |
| 2026-04-29 | Claude | Parallel scraping: 5 simultaneous scrape calls per niche, ~5x throughput | `app/api/cron/refresh-products/route.ts`, `vercel.json` |
| 2026-04-29 | Claude | SEO title expander, listing policy guard, smart feature bullets, stale pricing fix, fast bulk listing | `app/api/ebay/list-product/route.ts`, `lib/listing-policy.ts`, `lib/product-source-engine.ts` |
| 2026-04-30 | Codex | Rebuilt pricing into shared dynamic engine — cost-band ROI targets, psychological endings, competitor-aware publish | `lib/listing-pricing.ts`, all pricing consumers |
| 2026-04-30 | Codex | Fixed stale product-cache pricing, listing polish (badge, description), removed hard `PRICING_NOT_VIABLE` block | `app/api/scripts/product-finder/route.ts`, `app/api/ebay/list-product/route.ts`, `public/free-shipping-stamp.png` |
| 2026-04-29 | Codex | Added Product Source Health in Settings, source-engine, cache warm, cron integration | `app/api/product-source/health/route.ts`, `lib/product-source-engine.ts`, multiple |
| 2026-04-29 | Codex | Fixed Continuous Listing cold starts, 504 risk, product rotation, and dashboard theme | `app/api/scripts/product-finder/route.ts`, `app/dashboard/page.tsx`, `app/globals.css` |
| 2026-04-28 | Claude | Rebrand EbayDash → StackPilot; all tabs redesigned; emojis in sidebar; FedEx2Day shipping; eBay Taxonomy API | All tab components, `app/api/ebay/list-product/route.ts` |

---

## ⚠️ Flags for Other Agent

- **Three new 24/7 agents (2026-05-20)**: Reprice Agent (`:45 * * * *`), Fulfillment Agent (`*/20 * * * *`), Digest Agent (`0 6 * * *`) added to `vercel.json`. New DB tables: `reprice_agent_log`, `fulfillment_agent_tracker`, `performance_digests`. New columns: `listed_asins.last_repriced_at`, `product_source_items.ebay_competitor_count`, `product_source_items.listing_outcome_score`, `amazon_product_cache.best_seller_rank`. All created lazily via `ALTER TABLE IF NOT EXISTS` — no migration needed.
- **Sourcing score multipliers (2026-05-20)**: `scoreProduct()` in `product-source-engine.ts` now has BSR, eBay competition, and listing outcome multipliers on top of the existing stack. `applySourceIntelligenceScores()` SQL formula also includes all three. Do NOT remove — they improve sell-through by filtering saturated/poorly-performing products.
- **eBay competition enrichment (2026-05-20)**: `enrichCompetitionData()` in `lib/ebay-competition.ts` queries eBay Finding API and uses `EBAY_APP_ID` env var. Runs 20 products per `runSourceSelfHealing()` call. No extra cron needed.
- **Amazon connection**: REMOVED from Settings UI and page.tsx bootstrap. Do NOT add back — it doesn't work.
- **Agent roles**:
  - **Codex** = primary coder; `stackpilot-app.vercel.app` always points to Codex's latest deployment.
  - **Claude** = works in local Desktop copy, documents changes here for Codex to merge.
  - **Cursor** = active on this project; reads/writes COLLAB.md as source of truth.
  - **Antigravity** = active on this project; reads/writes COLLAB.md as source of truth.
  - All agents: read COLLAB.md before starting, update it when done.
- **NICHE_FALLBACK_LEAF_CATEGORY**: ✅ FIXED (2026-05-06) — all `29223` catch-alls replaced with real eBay leaf IDs. Do not revert.
- **Performance tab parallelization**: ✅ DONE — already runs in `Promise.all`. No action needed.
- **Overview greeting**: ✅ DONE (2026-05-06) — `"Good morning, [FirstName] 👋"` via `userName` prop in OverviewTab.
- **Category system**: Taxonomy REST API → ASIN Browse API → Legacy Trading API → NICHE_FALLBACK_LEAF_CATEGORY. Do NOT revert to `GetSuggestedCategories` as primary.
- **Shipping**: Default is `FedEx2Day` + `ExpeditedService: true`. Do NOT change back to USPSPriority.
- **Image pipeline**: Only `validatedAmazon.images` + provided `imageUrl` used in gallery. `fetchedAmazon.images` NEVER used for gallery — causes image contamination. Do not revert.
- **Free shipping stamp**: `public/free-shipping-stamp.png` was replaced — do not overwrite.
- **Brand name**: All UI is `StackPilot`. Vercel project = `stackpilot-app`. URL = `stackpilot-app.vercel.app`.
- **Get the app banner (2026-05-08)**: Shown on `/`, `/login`, `/signup`; on **desktop `/dashboard`** it sits in the **left sidebar** (under Operations Dashboard, above eBay status). **Compact PWA dashboard**: banner appears in **main** below `DashboardBanner` (sidebar is hidden). Hidden when dismissed (`localStorage` `stackpilot:get_the_app:dismissed:v2`) or standalone installed PWA. **`next/dynamic` + `ssr: false`**. **`?installHint=1`** clears dismiss for testing.
- **`NEXTAUTH_URL`** in Vercel = `https://stackpilot-app.vercel.app` — matches eBay Dev Console.
- **Product Finder distribution**: Per-user seeded ranking, large shared pool, performance signals. Do not revert.
- **Source intelligence layer (2026-05-13)**: `lib/source-intelligence.ts` owns source run logs, source-quality scoring, weak-niche recommendations, and self-healing. Catalog refreshes should call this layer rather than adding more Vercel cron jobs. Product Listing / Continuous / Auto Bulk should keep filtering `source_quality='reject'` and `amazon_product_cache.available = false`.
- **Source autopilot (2026-05-15)**: `/api/admin/stats` auto-detects weak niches/low queues and starts a throttled background repair via `/api/cron/refresh-products?autopilot=1`. Do not remove the `source_autopilot_locks` cooldown; it prevents duplicate deep crawls and Amazon rate-limit pain while still keeping the admin page proactive.
- **Source niche management (2026-05-12)**: Built-in trending niches live in `lib/source-niches.ts`. Admin-added custom niches are stored in `product_source_niches` and are merged into both Product Finder and the catalog cron. Use `/admin` Trending Niches to refresh one niche instead of hardcoding one-off query edits.
- **Fee rates**: `lib/listing-pricing.ts` is the shared engine — use it everywhere. `constants.ts` = 13.25% (display only). `listing-pricing.ts` = 15% actual. Do not introduce separate fee math.
- **Trusted mode**: Used for List All — skips Amazon re-validation. Uses cached `amazonPrice`. Do NOT disable — it's 3× faster for bulk listing.
- **Trusted mode pricing (2026-05-01)**: Always uses `pricingRecommendation.price` (fresh engine calc), not stale cached `ebayPrice`. Do NOT revert.
- **Trusted mode title/price accuracy (2026-05-03)**: After ASIN mismatch check, title = `cached.title`, price updated from cache if drift >10%. Do NOT revert.
- **ASIN mismatch guard — trusted mode (2026-05-03)**: Unconditional. Fires before supplement. 45% word overlap. Do NOT move inside `isDataSparse`.
- **ASIN mismatch guard — non-trusted (2026-05-02)**: <25% word overlap → `ASIN_MISMATCH`, listing blocked. Do NOT remove.
- **PRICE_BELOW_MARKET block (2026-05-03)**: `finalEbayPrice < competitor_median × 0.62` AND ≥3 data points → blocked. Do NOT raise the 0.62 threshold.
- **Duplicate listing block (2026-05-01)**: Checks `listed_asins` before calling eBay. Returns `ALREADY_LISTED (409)`. Do NOT remove — prevents eBay violations.
- **Unavailability sync (2026-05-01)**: Products confirmed unavailable auto-ended daily. Block at listing time. Do NOT revert.
- **fallbackImage (2026-05-02)**: Only used when source returned zero real images. NEVER appended unconditionally. Do NOT revert.
- **Bulk listing preflight (2026-05-06)**: `getBulkPreflightIssue` skips weak titles, bad prices, <2 images, low profit before eBay publish. Do NOT remove.
- **Live queue rotation (2026-05-06)**: 60-product pool, display 30, reshuffle client-side every second. Pauses during modal/bulk. Do NOT change to server-side 1s refresh.
- **Bulk concurrency**: `listProductsInBatches` called with `concurrency: 3`. Do NOT raise to 5 — causes Amazon rate limiting.
- **Background enrichment (2026-05-01)**: Up to 12 sparse products enriched via `after()` post-response. Do NOT await — defeats the purpose.
- **Daily cron (2026-05-02)**: `vercel.json` uses `?catalog=1&wait=1`. Do NOT revert to `?catalog=1` without `&wait=1` — that was a no-op for product discovery.
- **Supplements niche**: Can be listed EXCEPT controlled substances (kratom, ozempic, insulin, testosterone, CBD/THC, nicotine).
- **Cross-user dedup**: Product finder loads ALL active ASINs across all users (limit 2,000). Intentional. Do NOT revert to per-user only.
- **Campaigns tab**: Requires `sell.marketing` scope. Existing users must disconnect + reconnect eBay once. Yellow banner guides them.
- **Vercel deploy alias**: Verify `stackpilot-app.vercel.app` points to latest deploy after each push.
- **CI**: Runs `npm ci`, `npm run lint`, `npm run typecheck` on PRs and pushes to `main/master`.
- **AI route**: Requires `OPENROUTER_API_KEY`. Prompt length cap: 2400 chars. Model via `OPENROUTER_MODEL`.

---

## 📋 Queued Tasks from Mike

| Priority | Task | Notes |
|----------|------|-------|
| — | All queued tasks completed | See Recently Completed above |

---

## 📌 Architecture Notes

- **Category selection chain**: Taxonomy REST API → ASIN Browse API → Legacy Trading API → NICHE_FALLBACK_LEAF_CATEGORY → `29223` (last resort)
- **Image chain**: `validatedAmazon.images` + `imageUrl` merged → deduplicated → badge stamped on first image → EPS upload
- **Shipping**: FedEx2Day, ExpeditedService=true, FreeShipping=true, DispatchTimeMax=0
- **Pricing**: `lib/listing-pricing.ts` — dynamic cost-band ROI targets, eBay variable + fixed fee + buffer, psychological endings, competitor-aware final price
- **Niche cursor**: Stored in `product_cache` as `niche = '__cursor__'`. Advances 3 per catalog crawl. All 40 niches rotate every ~14 days.
- **Scraper**: `scrapeAmazonSearch` in `lib/amazon-scrape.ts` hits amazon.com directly. RapidAPI is optional fallback only and stays off unless `ENABLE_RAPIDAPI_FALLBACK=1`. 20 queries × 5 pages × 20 products = 2,500/niche.
- **Amazon badges stripped**: `sanitizeContent()` + `cleanTitle` strip Amazon Choice, Overall Pick, Best Seller, etc.

---

## 🔍 Production Audit (2026-05-08)

### Reviewed
- **Static checks**: ESLint + TypeScript typecheck (passing locally).
- **Build**: `next build` verified with CI placeholder env (matches GitHub Actions).
- **Cron + background jobs**: `vercel.json` crons, `/api/cron/*` routes, and Auto Bulk Listing scheduler.
- **Operational docs**: `docs/RELEASE_CHECKLIST.md` — objective manual + automated steps.

### Fixed
- **Cron auth hardening**: accept `x-vercel-cron: 1` to avoid 401s for scheduled invocations while still supporting `CRON_SECRET` bearer auth.
  - `app/api/cron/refresh-products/route.ts`
  - `app/api/cron/auto-listing-tick/route.ts`
- **SSRF guard**: locked `/api/image/proxy` to an allowlist of known image CDNs (Amazon/eBay) and HTTPS-only.
  - `app/api/image/proxy/route.ts`
- **DB indexes**: added composite index to support efficient “next job” selection for auto listing queue.
  - `lib/auto-listing/db.ts`
- **DX/typing cleanup**: removed `any` usage in Auto Bulk Listing dashboard client types.
  - `app/dashboard/api.ts`
  - `app/dashboard/components/SettingsTab.tsx`
- **Health endpoint**: `GET /api/health` for uptime / release verification (503 when DB or critical env missing).
  - `app/api/health/route.ts`

### Still needs testing
- **Manual (cannot automate without your seller session)**: sign-off rows in `docs/RELEASE_CHECKLIST.md` (login, eBay connect, orders, listing, fulfillment, admin, device PWA).
- **Auto Bulk Listing**: enable on staging/real account → confirm cron invocations + queue + listings (see checklist).
- **Post-deploy smoke**: `BASE_URL=https://stackpilot-app.vercel.app npm run smoke` after each production deploy.

### Risks / concerns
- Cron routes are now permissive for Vercel scheduler (`x-vercel-cron: 1`). If the route is reachable publicly, ensure only Vercel can set that header (expected) and that crons do not leak privileged operations beyond intended scope.
